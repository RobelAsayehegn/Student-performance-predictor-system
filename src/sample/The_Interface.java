package sample;

import java.sql.Connection;

public interface The_Interface {
    //this interface is for the method in the generic class is used for connecting to the database
    public<T> T Connector();
}
